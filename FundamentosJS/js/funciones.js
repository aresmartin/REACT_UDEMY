export const sumar = (n1, n2)=> n1 + n2

export const restar = (n1, n2)=> n1 - n2

//Solo puede haber un exportDefault por archivo
// ExportDefault se puede exportar y nombrar como queramos
export const multiplicar = (n1, n2) => n1 * n2
export const dividir = (n1, n2) => n1 / n2
//export default multiplicar
